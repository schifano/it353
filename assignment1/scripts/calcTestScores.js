// Given N test scores, write a JavaScript program to compute the (a) lowest, (b) highest, (c) average scores, and (d) list the scores that are higher than the average. Prompt the user for the value of N and the individual scores. You are to use the method "prompt" to do this question, not HTML form.

// Prompt user for test scores
// Indicate if user is finished
// Calculate lowest, highest, average
// List scores higher than average